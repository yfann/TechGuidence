# algorithm

## haspmap
+ 缓存中间结果，避免重复计算
+ 用于：
    + two sum

## 单调栈
+ 对每个元素，找它左右两侧最近的某种元素
    + 下一个更大/更小
    + 元素连续

+ 要素
    + 栈里一般存索引而不是值
    + 单调递减栈/单调递增栈
    + 当新元素“破坏了栈的单调性”时出栈

+ O(n)
    + 每个元素只入栈一次，出栈一次